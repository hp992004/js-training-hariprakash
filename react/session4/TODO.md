# TODO
- [x] Update `src/components/ScoreStats.tsx` to use `isLoading` from `useInterns()`
- [x] Render `Loading interns...` when loading
- [x] Keep existing stats rendering once loading completes
- [x] Run `npm test` (or `npm run test:run`) to verify `ScoreStats.test.tsx` passes


